-cp src
-lib heaps
-lib hlsdl
-D resourcePath=./res
-main Main
-hl Platformer.hl

# ==================================================================
# Default window size can be set here via command:
#     -D windowSize=1280x720
# I prefer we set it in code, either hard-coding, or we read it from
# a configuration file.
# ==================================================================
